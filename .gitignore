# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.playwright-mcp/

# Test artifacts / QA screenshots (Playwright & ad-hoc)
test-results/
/*.png
/*.jpeg

# 工作/临时目录（handoff 笔记、一次性 workflow 脚本、release notes 等）
.omo/

.superpowers/

# CodeGraph 本地索引库（机器本地、随 codegraph init 重建，不入库；
# .mcp.json 与 .claude/settings.json 是项目级接线，照常提交）
.codegraph/

# OpenCode 工具本地配置（lsp.json 里写了 C:/Users/USER 绝对路径；
# 与 .codegraph/.superpowers/.omo 同类机器本地状态，公开仓库不入库）
.opencode/

# 版权规则书临时抽取文本（设计调研用，绝不入库）
.tmp_combat_research/

# 设计阶段一次性预览页（HTML sketch，等 spec 落地即删，不入 build/commit）
tmp_preview/

# 剧本回填脚本中间产物（一次性 LLM 生成,人工核对后 paste 回 src/data/scenarios/<scn>.ts；
# 见 scripts/backfill-scenario-pools.ts 与 spec §7.1）
scripts/.backfill-output/
